# Atuin uses XDG paths automatically in this dotfiles setup.
# Sync remains opt-in until `atuin login` or `atuin register` is run.
auto_sync = false
sync_frequency = "5m"
search_mode = "fuzzy"
filter_mode = "global"
inline_height = 20
enter_accept = true
{{ if .development }}
sync_address = "https://{{ onepasswordRead "op://z25xttwluo345lyvannzyb7cuq/lqmx35k4aude3smpznltgb5ixy/x2qeplhndj5wq35744no6ajum4" }}"
{{ end }}
