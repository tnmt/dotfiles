# Atuin uses XDG paths automatically in this dotfiles setup.
# Sync remains opt-in until `atuin login` or `atuin register` is run.
auto_sync = true
sync_frequency = "20m"
search_mode = "fuzzy"
filter_mode = "global"
inline_height = 20
enter_accept = true
{{ if .development }}
sync_address = "https://{{ onepasswordRead "op://356ndxithkbsxtbzgunggh5cay/vrkvrowbafc3grt7g2pzkojwou/x2qeplhndj5wq35744no6ajum4" }}"
{{ end }}
