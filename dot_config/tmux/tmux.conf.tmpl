# Tmux Configuration

# =====================
# General Settings
# =====================

# Change prefix from C-b to C-t
unbind C-b
set-option -g prefix C-t
bind-key C-t send-prefix

# Use vi mode
setw -g mode-keys vi

# 24 hour clock
setw -g clock-mode-style 24

# History limit
set-option -g history-limit 10000

# Enable true color support
set-option -ga terminal-overrides ",$TERM:Tc"
set-option -g default-terminal "screen-256color"

# Renumber windows automatically
set-option -g renumber-windows on

# Disable mouse support
set -g mouse off

# No delay for escape key press
set -sg escape-time 0

# Increase repeat timeout
set -sg repeat-time 600

# =====================
# Key Bindings
# =====================

# Split panes using | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Resize panes with Ctrl-arrow
bind -r C-h resize-pane -L 5
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-l resize-pane -R 5

# Synchronize panes
bind e setw synchronize-panes on \; display "Synchronize: ON"
bind E setw synchronize-panes off \; display "Synchronize: OFF"

# Vi copy mode bindings
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "{{- if eq .chezmoi.os "darwin" }}pbcopy{{- else }}xclip -in -selection clipboard{{- end }}"
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle

# Quick window selection
bind -r C-p previous-window
bind -r C-n next-window

# =====================
# Status Bar
# =====================

# Update status bar interval (higher value reduces git operation conflicts)
set -g status-interval 10

# Center window list
set -g status-justify centre

# Enable activity alerts
setw -g monitor-activity on
set -g visual-activity off

# =====================
# Plugin Manager (TPM)
# =====================

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'janoamaral/tokyo-night-tmux'

# Plugin settings

# tokyo-night-tmux settings
set -g @tokyo-night-tmux_window_id_style none
set -g @tokyo-night-tmux_date_format YMD
set -g @tokyo-night-tmux_time_format 24H
set -g @tokyo-night-tmux_show_battery_widget 1
set -g @tokyo-night-tmux_battery_low_threshold 21

# =====================
# Initialize TPM
# =====================
# Keep this line at the very bottom of tmux.conf
run '~/.tmux/plugins/tpm/tpm'
